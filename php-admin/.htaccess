# PHP upload limits are set in .user.ini (this directory) — that file is the
# correct mechanism for LiteSpeed and PHP-FPM, which Namecheap cPanel uses.
# php_value directives are NOT used here because LiteSpeed ignores them and
# some hosts reject .htaccess files that contain them (causing a 500 error).
Options -Indexes
